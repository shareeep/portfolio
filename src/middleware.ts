// Minimal middleware, does nothing for now.
// You can re-enable or define new middleware logic if needed for other purposes.
export function middleware() {
  return undefined;
}

export const config = {
  matcher: [], // No routes matched by middleware for now
};
